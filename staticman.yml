# staticman.yml w katalogu głównym repozytorium
comments:
  allowedFields: ["name", "email", "message"]
  branch: "main"
  commitMessage: "Nowy komentarz"
  format: "yaml"
  moderation: true
  path: "_data/comments/{options.slug}"
  requiredFields: ["name", "message"]
