# Poprawny szablon konfiguracji
comments:
  allowedFields: ["name", "email", "message"]
  branch: "main"
  commitMessage: "Nowy komentarz"
  format: "yaml"
  path: "_data/comments/{options.slug}"
  requiredFields: 
    - name
    - message
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp-seconds"
