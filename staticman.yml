comments:
  allowedFields: 
    - name
    - email
    - message
  branch: "main"
  commitMessage: "Nowy komentarz: {fields.name}"
  path: "_data/comments/{options.slug}"
  filename: "{@id}"
  format: "yaml"
  moderation: true
  requiredFields: 
    - name
    - message
